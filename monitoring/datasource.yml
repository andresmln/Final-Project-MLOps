apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Since Grafana and Prometheus are in the same Docker Compose,
    # they talk internally via the container name "prometheus".
    url: http://localhost:9090
    isDefault: true
    editable: true